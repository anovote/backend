export enum BallotType {
  SINGLE,
  MULTIPLE,
  RANKED,
}

export enum BallotResultDisplay {
  NONE,
  SINGLE,
  RUNNER_UP,
  ALL,
}

export enum BallotStatus {
  IN_QUEUE,
  IN_PROGRESS,
  IN_RESULT,
  IN_ARCHIVE,
}

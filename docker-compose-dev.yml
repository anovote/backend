version: '3.9'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: source
    volumes:
      - './src:/app/src'
      - './logs:/app/logs'
  db:
    ports:
      - '${DB_PORT}:${DB_PORT}'
